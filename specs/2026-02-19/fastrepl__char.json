{
  "schema_version": 3,
  "app": {
    "name": "Char",
    "one_liner": "An AI notepad for private meetings that listens to audio and generates summaries based on user memos."
  },
  "screens": [
    {
      "name": "Main Notepad",
      "purpose": "Allows users to take notes during meetings while Char captures audio in the background.",
      "primary_actions": [
        "Generate post-meeting summary",
        "Jot down memo",
        "View real-time transcription"
      ]
    }
  ],
  "rust_commands": [
    {
      "name": "save_item",
      "purpose": "Persist a memo or note item to local storage.",
      "async": true,
      "input": {
        "memos": "json"
      },
      "output": {
        "created": "int",
        "id": "string",
        "ok": "boolean"
      }
    }
  ],
  "data_model": {
    "tables": [
      {
        "name": "memos",
        "columns": [
          {
            "name": "content",
            "type": "TEXT"
          },
          {
            "name": "id",
            "type": "TEXT"
          },
          {
            "name": "timestamp",
            "type": "DATETIME"
          }
        ]
      }
    ]
  },
  "mvp_plan": [
    "week 1: Create main notepad UI",
    "week 1: Implement basic audio capture",
    "week 1: Set up Tauri + Rust backend"
  ],
  "acceptance_tests": [
    "After ending a session, a summary is generated using local LLM (e.g., Ollama).",
    "User can open the app and start taking notes during a simulated meeting."
  ]
}
