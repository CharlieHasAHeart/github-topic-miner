{
  "schema_version": 3,
  "app": {
    "name": "vue-tui",
    "one_liner": "A terminal UI framework built on Vue 3 and Vite that enables component-based TUI development with HMR, flexbox layout, and integrated testing."
  },
  "screens": [
    {
      "name": "Main Application Screen",
      "purpose": "Render the primary terminal user interface composed of Vue components with reactive state and structured layout.",
      "primary_actions": [
        "Handle keyboard navigation and input",
        "Render UI using Vue SFC/TSX components",
        "Update layout via Yoga-powered flexbox"
      ]
    }
  ],
  "rust_commands": [
    {
      "name": "save_item",
      "purpose": "Persist terminal UI state or configuration (placeholder; project is TypeScript-based, not Rust).",
      "async": true,
      "input": {
        "payload": "json",
        "template": "string"
      },
      "output": {
        "ok": "boolean",
        "result": "json?"
      }
    }
  ],
  "data_model": {
    "tables": [
      {
        "name": "components",
        "columns": [
          {
            "name": "id",
            "type": "TEXT"
          },
          {
            "name": "template",
            "type": "TEXT"
          }
        ]
      }
    ]
  },
  "mvp_plan": [
    "week 1: Configure HMR and verify terminal rendering",
    "week 1: Implement basic App.vue with Box and Text components",
    "week 1: Set up vue-tui project using starter template",
    "week 1: Write first acceptance test using @vue-tui/testing"
  ],
  "acceptance_tests": [
    "The application renders a 'Hello World' message in the terminal using a Vue component.",
    "User can navigate between interactive elements using Tab key and keyboard input."
  ]
}
